# Generated by packages/oday_data_product_contracts_client/codegen.py -- do not edit.
#
# Contract:  emgi.coverage-surface.v1 (decision_product, v1.0.0)
# Release:   oday-data-product-contracts.v0.4.1
# Producer:  alfloop-dev/oday-data-platform@245aa00e417b6f8450baa608e8976584e00be6fc
# Schema:    schemas/coverage-surface.schema.json
# Digest:    d127f9dc57b38a70fbba9d55132bb561ad5127ae30eeeb8e31ae1408ae09896c
"""Canonical contract schemas for published domain coverage surfaces (emgi.coverage-surface.v1) and addressable data gap products (emgi.data-gap.v1) across space, time, freshness, and readiness reasons in EMGI v0.4.1."""

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass, field
from typing import Any

CONTRACT_ID = "emgi.coverage-surface.v1"
CONTRACT_VERSION = "1.0.0"
CONTRACT_CATEGORY = "decision_product"
SCHEMA_FILE = "schemas/coverage-surface.schema.json"
SCHEMA_SHA256 = "d127f9dc57b38a70fbba9d55132bb561ad5127ae30eeeb8e31ae1408ae09896c"
ROOT_MODEL = "CoverageSurface"


@dataclass(frozen=True, slots=True)
class CoverageCell:
    """Released contract model CoverageCell."""

    cell_id: str
    is_complete: bool
    negative_evidence_valid: bool
    readiness: str
    state: str
    admin_code: str | None = None
    age_seconds: int | None = None
    business_date: str | None = None
    coverage_ids: list[str] = field(default_factory=list)
    entity_id: str | None = None
    entity_type: str | None = None
    expected_count: int | None = None
    freshness_state: str = "not_applicable"
    h3_index: str | None = None
    knowledge_as_of: str | None = None
    observed_count: int = 0
    quarantine_reason: str | None = None
    query_geometry: QueryGeometry | None = None
    reasons: list[ReadinessReason] = field(default_factory=list)
    search_execution_ids: list[str] = field(default_factory=list)
    time_window: TimeWindow | None = None

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> CoverageCell:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["cell_id"] = data["cell_id"]
        values["is_complete"] = data["is_complete"]
        values["negative_evidence_valid"] = data["negative_evidence_valid"]
        values["readiness"] = data["readiness"]
        values["state"] = data["state"]
        if "admin_code" in data:
            values["admin_code"] = data["admin_code"]
        if "age_seconds" in data:
            values["age_seconds"] = data["age_seconds"]
        if "business_date" in data:
            values["business_date"] = data["business_date"]
        if "coverage_ids" in data:
            values["coverage_ids"] = list(data["coverage_ids"])
        if "entity_id" in data:
            values["entity_id"] = data["entity_id"]
        if "entity_type" in data:
            values["entity_type"] = data["entity_type"]
        if "expected_count" in data:
            values["expected_count"] = data["expected_count"]
        if "freshness_state" in data:
            values["freshness_state"] = data["freshness_state"]
        if "h3_index" in data:
            values["h3_index"] = data["h3_index"]
        if "knowledge_as_of" in data:
            values["knowledge_as_of"] = data["knowledge_as_of"]
        if "observed_count" in data:
            values["observed_count"] = data["observed_count"]
        if "quarantine_reason" in data:
            values["quarantine_reason"] = data["quarantine_reason"]
        if "query_geometry" in data:
            values["query_geometry"] = None if data["query_geometry"] is None else QueryGeometry.from_dict(data["query_geometry"])
        if "reasons" in data:
            values["reasons"] = [ReadinessReason.from_dict(item) for item in data["reasons"]]
        if "search_execution_ids" in data:
            values["search_execution_ids"] = list(data["search_execution_ids"])
        if "time_window" in data:
            values["time_window"] = None if data["time_window"] is None else TimeWindow.from_dict(data["time_window"])
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["cell_id"] = self.cell_id
        data["is_complete"] = self.is_complete
        data["negative_evidence_valid"] = self.negative_evidence_valid
        data["readiness"] = self.readiness
        data["state"] = self.state
        if self.admin_code is not None:
            data["admin_code"] = self.admin_code
        if self.age_seconds is not None:
            data["age_seconds"] = self.age_seconds
        if self.business_date is not None:
            data["business_date"] = self.business_date
        data["coverage_ids"] = list(self.coverage_ids)
        if self.entity_id is not None:
            data["entity_id"] = self.entity_id
        if self.entity_type is not None:
            data["entity_type"] = self.entity_type
        if self.expected_count is not None:
            data["expected_count"] = self.expected_count
        data["freshness_state"] = self.freshness_state
        if self.h3_index is not None:
            data["h3_index"] = self.h3_index
        if self.knowledge_as_of is not None:
            data["knowledge_as_of"] = self.knowledge_as_of
        data["observed_count"] = self.observed_count
        if self.quarantine_reason is not None:
            data["quarantine_reason"] = self.quarantine_reason
        if self.query_geometry is not None:
            data["query_geometry"] = self.query_geometry.to_dict()
        data["reasons"] = [item.to_dict() for item in self.reasons]
        data["search_execution_ids"] = list(self.search_execution_ids)
        if self.time_window is not None:
            data["time_window"] = self.time_window.to_dict()
        return data


@dataclass(frozen=True, slots=True)
class CoverageProductRelease:
    """Released contract model CoverageProductRelease."""

    gaps: DataGapDocument
    surface: CoverageSurface

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> CoverageProductRelease:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["gaps"] = DataGapDocument.from_dict(data["gaps"])
        values["surface"] = CoverageSurface.from_dict(data["surface"])
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["gaps"] = self.gaps.to_dict()
        data["surface"] = self.surface.to_dict()
        return data


@dataclass(frozen=True, slots=True)
class CoverageStateBreakdown:
    """Released contract model CoverageStateBreakdown."""

    complete: int = 0
    empty: int = 0
    partial: int = 0
    saturated: int = 0
    source_error: int = 0
    truncated: int = 0
    unknown: int = 0

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> CoverageStateBreakdown:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        if "complete" in data:
            values["complete"] = data["complete"]
        if "empty" in data:
            values["empty"] = data["empty"]
        if "partial" in data:
            values["partial"] = data["partial"]
        if "saturated" in data:
            values["saturated"] = data["saturated"]
        if "source_error" in data:
            values["source_error"] = data["source_error"]
        if "truncated" in data:
            values["truncated"] = data["truncated"]
        if "unknown" in data:
            values["unknown"] = data["unknown"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["complete"] = self.complete
        data["empty"] = self.empty
        data["partial"] = self.partial
        data["saturated"] = self.saturated
        data["source_error"] = self.source_error
        data["truncated"] = self.truncated
        data["unknown"] = self.unknown
        return data


@dataclass(frozen=True, slots=True)
class CoverageSurface:
    """Released contract model CoverageSurface."""

    blockers: list[DataGap]
    cells: list[CoverageCell]
    dataset_ids: list[str]
    domain: str
    freshness: FreshnessSummary
    generated_at: str
    product_version: str
    readiness: str
    readiness_reasons: list[ReadinessReason]
    scope_principal_id: str
    search_completeness: SearchCompletenessSummary
    spatial_grain: str
    state_breakdown: CoverageStateBreakdown
    surface_id: str
    temporal_grain: str
    contract_version: str = "emgi.coverage-surface.v1"
    h3_resolution: int | None = None
    knowledge_as_of: str | None = None
    metadata: dict[str, Any] = field(default_factory=dict)
    store_timezone: str = "Asia/Taipei"
    tenant_id: str | None = None
    time_window: TimeWindow | None = None
    withheld_coverage_count: int = 0

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> CoverageSurface:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["blockers"] = [DataGap.from_dict(item) for item in data["blockers"]]
        values["cells"] = [CoverageCell.from_dict(item) for item in data["cells"]]
        values["dataset_ids"] = list(data["dataset_ids"])
        values["domain"] = data["domain"]
        values["freshness"] = FreshnessSummary.from_dict(data["freshness"])
        values["generated_at"] = data["generated_at"]
        values["product_version"] = data["product_version"]
        values["readiness"] = data["readiness"]
        values["readiness_reasons"] = [ReadinessReason.from_dict(item) for item in data["readiness_reasons"]]
        values["scope_principal_id"] = data["scope_principal_id"]
        values["search_completeness"] = SearchCompletenessSummary.from_dict(data["search_completeness"])
        values["spatial_grain"] = data["spatial_grain"]
        values["state_breakdown"] = CoverageStateBreakdown.from_dict(data["state_breakdown"])
        values["surface_id"] = data["surface_id"]
        values["temporal_grain"] = data["temporal_grain"]
        if "contract_version" in data:
            values["contract_version"] = data["contract_version"]
        if "h3_resolution" in data:
            values["h3_resolution"] = data["h3_resolution"]
        if "knowledge_as_of" in data:
            values["knowledge_as_of"] = data["knowledge_as_of"]
        if "metadata" in data:
            values["metadata"] = data["metadata"]
        if "store_timezone" in data:
            values["store_timezone"] = data["store_timezone"]
        if "tenant_id" in data:
            values["tenant_id"] = data["tenant_id"]
        if "time_window" in data:
            values["time_window"] = None if data["time_window"] is None else TimeWindow.from_dict(data["time_window"])
        if "withheld_coverage_count" in data:
            values["withheld_coverage_count"] = data["withheld_coverage_count"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["blockers"] = [item.to_dict() for item in self.blockers]
        data["cells"] = [item.to_dict() for item in self.cells]
        data["dataset_ids"] = list(self.dataset_ids)
        data["domain"] = self.domain
        data["freshness"] = self.freshness.to_dict()
        data["generated_at"] = self.generated_at
        data["product_version"] = self.product_version
        data["readiness"] = self.readiness
        data["readiness_reasons"] = [item.to_dict() for item in self.readiness_reasons]
        data["scope_principal_id"] = self.scope_principal_id
        data["search_completeness"] = self.search_completeness.to_dict()
        data["spatial_grain"] = self.spatial_grain
        data["state_breakdown"] = self.state_breakdown.to_dict()
        data["surface_id"] = self.surface_id
        data["temporal_grain"] = self.temporal_grain
        data["contract_version"] = self.contract_version
        if self.h3_resolution is not None:
            data["h3_resolution"] = self.h3_resolution
        if self.knowledge_as_of is not None:
            data["knowledge_as_of"] = self.knowledge_as_of
        data["metadata"] = self.metadata
        data["store_timezone"] = self.store_timezone
        if self.tenant_id is not None:
            data["tenant_id"] = self.tenant_id
        if self.time_window is not None:
            data["time_window"] = self.time_window.to_dict()
        data["withheld_coverage_count"] = self.withheld_coverage_count
        return data


@dataclass(frozen=True, slots=True)
class DataGap:
    """Released contract model DataGap."""

    blocking: bool
    dataset_id: str
    detail: str
    detected_at: str
    domain: str
    gap_id: str
    gap_kind: str
    reason_code: str
    remediation: str
    scope_principal_id: str
    age_seconds: int | None = None
    business_date: str | None = None
    cell_id: str | None = None
    contract_version: str = "emgi.data-gap.v1"
    coverage_ids: list[str] = field(default_factory=list)
    dataset_version_id: str | None = None
    entity_id: str | None = None
    entity_type: str | None = None
    expected_count: int | None = None
    knowledge_as_of: str | None = None
    metadata: dict[str, Any] = field(default_factory=dict)
    observed_count: int = 0
    query_geometry: QueryGeometry | None = None
    search_execution_ids: list[str] = field(default_factory=list)
    surface_id: str | None = None
    time_window: TimeWindow | None = None

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> DataGap:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["blocking"] = data["blocking"]
        values["dataset_id"] = data["dataset_id"]
        values["detail"] = data["detail"]
        values["detected_at"] = data["detected_at"]
        values["domain"] = data["domain"]
        values["gap_id"] = data["gap_id"]
        values["gap_kind"] = data["gap_kind"]
        values["reason_code"] = data["reason_code"]
        values["remediation"] = data["remediation"]
        values["scope_principal_id"] = data["scope_principal_id"]
        if "age_seconds" in data:
            values["age_seconds"] = data["age_seconds"]
        if "business_date" in data:
            values["business_date"] = data["business_date"]
        if "cell_id" in data:
            values["cell_id"] = data["cell_id"]
        if "contract_version" in data:
            values["contract_version"] = data["contract_version"]
        if "coverage_ids" in data:
            values["coverage_ids"] = list(data["coverage_ids"])
        if "dataset_version_id" in data:
            values["dataset_version_id"] = data["dataset_version_id"]
        if "entity_id" in data:
            values["entity_id"] = data["entity_id"]
        if "entity_type" in data:
            values["entity_type"] = data["entity_type"]
        if "expected_count" in data:
            values["expected_count"] = data["expected_count"]
        if "knowledge_as_of" in data:
            values["knowledge_as_of"] = data["knowledge_as_of"]
        if "metadata" in data:
            values["metadata"] = data["metadata"]
        if "observed_count" in data:
            values["observed_count"] = data["observed_count"]
        if "query_geometry" in data:
            values["query_geometry"] = None if data["query_geometry"] is None else QueryGeometry.from_dict(data["query_geometry"])
        if "search_execution_ids" in data:
            values["search_execution_ids"] = list(data["search_execution_ids"])
        if "surface_id" in data:
            values["surface_id"] = data["surface_id"]
        if "time_window" in data:
            values["time_window"] = None if data["time_window"] is None else TimeWindow.from_dict(data["time_window"])
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["blocking"] = self.blocking
        data["dataset_id"] = self.dataset_id
        data["detail"] = self.detail
        data["detected_at"] = self.detected_at
        data["domain"] = self.domain
        data["gap_id"] = self.gap_id
        data["gap_kind"] = self.gap_kind
        data["reason_code"] = self.reason_code
        data["remediation"] = self.remediation
        data["scope_principal_id"] = self.scope_principal_id
        if self.age_seconds is not None:
            data["age_seconds"] = self.age_seconds
        if self.business_date is not None:
            data["business_date"] = self.business_date
        if self.cell_id is not None:
            data["cell_id"] = self.cell_id
        data["contract_version"] = self.contract_version
        data["coverage_ids"] = list(self.coverage_ids)
        if self.dataset_version_id is not None:
            data["dataset_version_id"] = self.dataset_version_id
        if self.entity_id is not None:
            data["entity_id"] = self.entity_id
        if self.entity_type is not None:
            data["entity_type"] = self.entity_type
        if self.expected_count is not None:
            data["expected_count"] = self.expected_count
        if self.knowledge_as_of is not None:
            data["knowledge_as_of"] = self.knowledge_as_of
        data["metadata"] = self.metadata
        data["observed_count"] = self.observed_count
        if self.query_geometry is not None:
            data["query_geometry"] = self.query_geometry.to_dict()
        data["search_execution_ids"] = list(self.search_execution_ids)
        if self.surface_id is not None:
            data["surface_id"] = self.surface_id
        if self.time_window is not None:
            data["time_window"] = self.time_window.to_dict()
        return data


@dataclass(frozen=True, slots=True)
class DataGapDocument:
    """Released contract model DataGapDocument."""

    generated_at: str
    contract_version: str = "emgi.data-gap.v1"
    gaps: list[DataGap] = field(default_factory=list)
    metadata: dict[str, Any] = field(default_factory=dict)
    surface_id: str | None = None

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> DataGapDocument:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["generated_at"] = data["generated_at"]
        if "contract_version" in data:
            values["contract_version"] = data["contract_version"]
        if "gaps" in data:
            values["gaps"] = [DataGap.from_dict(item) for item in data["gaps"]]
        if "metadata" in data:
            values["metadata"] = data["metadata"]
        if "surface_id" in data:
            values["surface_id"] = data["surface_id"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["generated_at"] = self.generated_at
        data["contract_version"] = self.contract_version
        data["gaps"] = [item.to_dict() for item in self.gaps]
        data["metadata"] = self.metadata
        if self.surface_id is not None:
            data["surface_id"] = self.surface_id
        return data


@dataclass(frozen=True, slots=True)
class FreshnessSummary:
    """Released contract model FreshnessSummary."""

    evaluated_at: str
    state: str
    max_age_seconds: int | None = None
    newest_knowledge_as_of: str | None = None
    oldest_knowledge_as_of: str | None = None
    sla_seconds: int | None = None
    stale_cell_count: int = 0
    unknown_cell_count: int = 0

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> FreshnessSummary:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["evaluated_at"] = data["evaluated_at"]
        values["state"] = data["state"]
        if "max_age_seconds" in data:
            values["max_age_seconds"] = data["max_age_seconds"]
        if "newest_knowledge_as_of" in data:
            values["newest_knowledge_as_of"] = data["newest_knowledge_as_of"]
        if "oldest_knowledge_as_of" in data:
            values["oldest_knowledge_as_of"] = data["oldest_knowledge_as_of"]
        if "sla_seconds" in data:
            values["sla_seconds"] = data["sla_seconds"]
        if "stale_cell_count" in data:
            values["stale_cell_count"] = data["stale_cell_count"]
        if "unknown_cell_count" in data:
            values["unknown_cell_count"] = data["unknown_cell_count"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["evaluated_at"] = self.evaluated_at
        data["state"] = self.state
        if self.max_age_seconds is not None:
            data["max_age_seconds"] = self.max_age_seconds
        if self.newest_knowledge_as_of is not None:
            data["newest_knowledge_as_of"] = self.newest_knowledge_as_of
        if self.oldest_knowledge_as_of is not None:
            data["oldest_knowledge_as_of"] = self.oldest_knowledge_as_of
        if self.sla_seconds is not None:
            data["sla_seconds"] = self.sla_seconds
        data["stale_cell_count"] = self.stale_cell_count
        data["unknown_cell_count"] = self.unknown_cell_count
        return data


@dataclass(frozen=True, slots=True)
class QueryGeometry:
    """Released contract model QueryGeometry."""

    admin_code: str | None = None
    coordinates: Any = None
    geometry_type: str = "NONE"
    h3_index: str | None = None
    srid: int = 4326

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> QueryGeometry:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        if "admin_code" in data:
            values["admin_code"] = data["admin_code"]
        if "coordinates" in data:
            values["coordinates"] = data["coordinates"]
        if "geometry_type" in data:
            values["geometry_type"] = data["geometry_type"]
        if "h3_index" in data:
            values["h3_index"] = data["h3_index"]
        if "srid" in data:
            values["srid"] = data["srid"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        if self.admin_code is not None:
            data["admin_code"] = self.admin_code
        if self.coordinates is not None:
            data["coordinates"] = self.coordinates
        data["geometry_type"] = self.geometry_type
        if self.h3_index is not None:
            data["h3_index"] = self.h3_index
        data["srid"] = self.srid
        return data


@dataclass(frozen=True, slots=True)
class ReadinessReason:
    """Released contract model ReadinessReason."""

    code: str
    detail: str
    severity: str
    affected_cell_count: int = 0
    coverage_ids: list[str] = field(default_factory=list)
    gap_ids: list[str] = field(default_factory=list)

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> ReadinessReason:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["code"] = data["code"]
        values["detail"] = data["detail"]
        values["severity"] = data["severity"]
        if "affected_cell_count" in data:
            values["affected_cell_count"] = data["affected_cell_count"]
        if "coverage_ids" in data:
            values["coverage_ids"] = list(data["coverage_ids"])
        if "gap_ids" in data:
            values["gap_ids"] = list(data["gap_ids"])
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["code"] = self.code
        data["detail"] = self.detail
        data["severity"] = self.severity
        data["affected_cell_count"] = self.affected_cell_count
        data["coverage_ids"] = list(self.coverage_ids)
        data["gap_ids"] = list(self.gap_ids)
        return data


@dataclass(frozen=True, slots=True)
class SearchCompletenessSummary:
    """Released contract model SearchCompletenessSummary."""

    complete: int = 0
    partial: int = 0
    refinement_required: bool = False
    saturated: int = 0
    saturated_partition_keys: list[str] = field(default_factory=list)
    source_error: int = 0
    truncated: int = 0
    truncated_partition_keys: list[str] = field(default_factory=list)
    unknown: int = 0

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> SearchCompletenessSummary:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        if "complete" in data:
            values["complete"] = data["complete"]
        if "partial" in data:
            values["partial"] = data["partial"]
        if "refinement_required" in data:
            values["refinement_required"] = data["refinement_required"]
        if "saturated" in data:
            values["saturated"] = data["saturated"]
        if "saturated_partition_keys" in data:
            values["saturated_partition_keys"] = list(data["saturated_partition_keys"])
        if "source_error" in data:
            values["source_error"] = data["source_error"]
        if "truncated" in data:
            values["truncated"] = data["truncated"]
        if "truncated_partition_keys" in data:
            values["truncated_partition_keys"] = list(data["truncated_partition_keys"])
        if "unknown" in data:
            values["unknown"] = data["unknown"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["complete"] = self.complete
        data["partial"] = self.partial
        data["refinement_required"] = self.refinement_required
        data["saturated"] = self.saturated
        data["saturated_partition_keys"] = list(self.saturated_partition_keys)
        data["source_error"] = self.source_error
        data["truncated"] = self.truncated
        data["truncated_partition_keys"] = list(self.truncated_partition_keys)
        data["unknown"] = self.unknown
        return data


@dataclass(frozen=True, slots=True)
class TimeWindow:
    """Released contract model TimeWindow."""

    window_end: str
    window_start: str

    @classmethod
    def from_dict(cls, data: Mapping[str, Any]) -> TimeWindow:
        """Build the model from one released-contract JSON object."""
        values: dict[str, Any] = {}
        values["window_end"] = data["window_end"]
        values["window_start"] = data["window_start"]
        return cls(**values)

    def to_dict(self) -> dict[str, Any]:
        """Render the model back to its wire form.

        Schema-optional fields that are `None` are omitted rather than
        emitted as nulls, because not every optional field is nullable.
        """
        data: dict[str, Any] = {}
        data["window_end"] = self.window_end
        data["window_start"] = self.window_start
        return data

