{
  "_comment": "Settings declared in config.example.json that no code reads yet. Each entry needs a reason. Wire the setting and delete the entry, or delete the setting. Enforced by scripts/check_config_wiring.py, which also fails when an entry here becomes wired, so this file cannot drift out of date. This list is the backlog of governance the config promises but the system does not implement.",
  "unwired": {
    "github_bus.phase3.github_app": "GitHub App auth mode is phase 3 and unimplemented; the bus authenticates through the gh CLI.",
    "github_bus.phase3.github_app.enabled": "See github_bus.phase3.github_app.",
    "github_bus.phase3.github_app.app_id_env": "See github_bus.phase3.github_app.",
    "github_bus.phase3.github_app.private_key_env": "See github_bus.phase3.github_app.",
    "github_bus.phase3.github_app.installation_id_env": "See github_bus.phase3.github_app.",
    "github_bus.phase3.cloud_relay.enabled": "Cloud relay is phase 3 and unimplemented; nothing posts to it.",
    "branch_workflow.task_pr.required_status_checks": "Required checks are defined by GitHub branch protection on dev, not from config. The two lists are maintained independently and already differ.",
    "branch_workflow.task_pr.max_open_hours": "Nothing tracks how long a task PR stays open. #588 sat for over a day and #589 for two days with no signal.",
    "branch_workflow.drift_alarms.publish_must_promote_within_minutes": "The publish/release pipeline was never built, so there is no publish branch whose promotion could be timed. Wiring a threshold that cannot fail would read as one being met. The other two thresholds in this block are implemented in .orchestrator/branch_drift_alarms.py.",
    "branch_workflow.orchestrator_sync_webhook": "Disabled by default and never implemented; no code posts to it.",
    "branch_workflow.orchestrator_sync_webhook.enabled": "See branch_workflow.orchestrator_sync_webhook.",
    "branch_workflow.orchestrator_sync_webhook.url_env": "See branch_workflow.orchestrator_sync_webhook.",
    "branch_workflow.orchestrator_sync_webhook.secret_env": "See branch_workflow.orchestrator_sync_webhook."
  }
}
