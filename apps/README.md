# Apps

Runtime entrypoints live here:

- `api`: core API and BFF
- `web`: OpsBoard web shell
- `worker`: event consumers and asynchronous job workers
- `scheduler`: scheduled triggers
- `cli`: admin and migration commands
