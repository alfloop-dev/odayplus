diff --git a/src/oday_data_platform/defs/external/mof_moi.py b/src/oday_data_platform/defs/external/mof_moi.py
index 69cbe31..35b9445 100644
--- a/src/oday_data_platform/defs/external/mof_moi.py
+++ b/src/oday_data_platform/defs/external/mof_moi.py
@@ -7,11 +7,13 @@ contract and acquisition receipt -- deliberately no record counts, so nothing
 downstream can mistake a registration feed for store truth.
 """
 
+from datetime import datetime, timezone
+from functools import partial
+from uuid import uuid4
+
 import dagster as dg
 from dagster import AssetExecutionContext
 
-from datetime import datetime, timezone
-
 from oday_data_platform.defs.external.live_source import (
     acquire_release_bytes,
     build_kernel,
@@ -20,8 +22,6 @@ from oday_data_platform.defs.external.live_source import (
     live_receipt,
     optional_credential,
 )
-from oday_data_platform.infra.resources import ExternalSourceResource
-
 from oday_data_platform.external.registry.models import (
     PublicationState,
     SourceRegistryDocument,
@@ -45,9 +45,13 @@ from oday_data_platform.external.sources.moi_rental import (
     create_moi_rental_registry_definitions,
     moi_rental_schema_sha256,
 )
+from oday_data_platform.infra.outbound_control import provider_tls_context
+from oday_data_platform.infra.resources import ExternalSourceResource
 
 SCOPE_PRINCIPAL_ID = "principal.platform.public"
 MOF_ADAPTER_VERSION = "1.0.0"
+MOF_PAGE_LIMIT = 100
+MOF_PAGE_OFFSET = 0
 MOI_RENTAL_ADAPTER_VERSION = "1.0.0"
 
 
@@ -78,7 +82,7 @@ def mof_business_registrations_raw(
     context: AssetExecutionContext,
     external_sources: ExternalSourceResource,
 ) -> dg.MaterializeResult:
-    """Acquire one MOF business tax registration release and publish its receipt."""
+    """Acquire the first bounded MOF API page and publish its receipt."""
     context.log.info(
         "MOF business registration source asset initialized under contract %s (%s)",
         MOF_CONTRACT_ID,
@@ -86,6 +90,7 @@ def mof_business_registrations_raw(
     )
 
     release_key = datetime.now(timezone.utc).strftime("%Y-%m")
+    capture_id = f"{release_key}.{uuid4().hex}"
     mof_p, mof_d, mof_c, mof_v, mof_a = create_mof_registry_definitions(release_key=release_key)
     registry_doc = SourceRegistryDocument(
         providers=(mof_p,),
@@ -100,7 +105,7 @@ def mof_business_registrations_raw(
         registry=registry_doc,
         store=store,
         scope_principal_id=SCOPE_PRINCIPAL_ID,
-        client_factory=external_sources.client,
+        client_factory=partial(external_sources.client, verify=provider_tls_context()),
         base_timeout_seconds=external_sources.base_timeout_seconds,
         max_retries=external_sources.max_retries,
     )
@@ -112,17 +117,21 @@ def mof_business_registrations_raw(
             scope_principal_id=SCOPE_PRINCIPAL_ID,
         ),
         dataset_version_id=mof_v.dataset_version_id,
-        execution_id=f"exec.mof.business.{release_key}",
+        execution_id=f"exec.mof.business.{capture_id}",
         scope_principal_id=SCOPE_PRINCIPAL_ID,
         url=mof_v.source_uri,
-        blob_id=f"blob.mof.business.{release_key}",
-        storage_uri=f"mof://business/{release_key}.csv",
+        blob_id=f"blob.mof.business.{capture_id}",
+        storage_uri=f"mof://business/{capture_id}.json",
         headers=optional_credential("MOF_API_KEY"),
+        params={"limit": MOF_PAGE_LIMIT, "offset": MOF_PAGE_OFFSET},
+        result_limit=MOF_PAGE_LIMIT,
     )
 
     return dg.MaterializeResult(
         metadata={
             "provider_id": MOF_PROVIDER_ID,
+            "capture_id": capture_id,
+            "acquisition_scope": "first_page_only",
             "dataset_id": MOF_DATASET_ID,
             "contract_id": MOF_CONTRACT_ID,
             "contract_version_id": MOF_CONTRACT_VERSION_ID,
diff --git a/src/oday_data_platform/defs/external/ris_nlsc.py b/src/oday_data_platform/defs/external/ris_nlsc.py
index ec50aa8..d0d439e 100644
--- a/src/oday_data_platform/defs/external/ris_nlsc.py
+++ b/src/oday_data_platform/defs/external/ris_nlsc.py
@@ -7,9 +7,11 @@ fusion asset consumes those bytes; it deliberately does not re-derive them, so
 a join can never be computed over inputs the releases did not contain.
 """
 
+import json
 import os
 from datetime import datetime, timezone
 from typing import Any
+from uuid import uuid4
 
 import dagster as dg
 
@@ -24,6 +26,7 @@ from oday_data_platform.defs.external.live_source import (
 )
 from oday_data_platform.external.registry.models import (
     PolicyState,
+    Provider,
     PublicationState,
     ReleaseKind,
     SourceAdapter,
@@ -33,13 +36,17 @@ from oday_data_platform.external.registry.models import (
     SourceRegistryDocument,
     SourceRole,
     TechnicalReadiness,
-    Provider,
 )
 from oday_data_platform.external.sources.nlsc.adapter import NLSCAdapter
 from oday_data_platform.external.sources.nlsc.models import nlsc_boundary_schema_sha256
 from oday_data_platform.external.sources.ris.adapter import RISAdapter
 from oday_data_platform.external.sources.ris.join import AdministrativeJoinEngine
 from oday_data_platform.external.sources.ris.models import ris_population_schema_sha256
+from oday_data_platform.external.sources.ris.official import (
+    last_completed_month,
+    ris_response_rows,
+    ris_source_uri,
+)
 from oday_data_platform.infra.geospatial import GeospatialResource
 from oday_data_platform.infra.resources import ExternalSourceResource
 
@@ -48,7 +55,7 @@ SCOPE_PRINCIPAL_ID = "principal.platform.public"
 RIS_PROVIDER_ID = "provider.moi.ris"
 RIS_DATASET_ID = "dataset.ris.population"
 RIS_CONTRACT_VERSION_ID = "contract_version.ris.v1"
-RIS_SOURCE_URI = "https://data.moi.gov.tw/MoiOD/System/DownloadFile.aspx?DATA=ODRP014"
+RIS_SOURCE_URI = "https://www.ris.gov.tw/rs-opendata/api/v1/datastore/ODRP014"
 
 NLSC_PROVIDER_ID = "provider.moi.nlsc"
 NLSC_DATASET_ID = "dataset.nlsc.boundary"
@@ -65,7 +72,10 @@ def _release_key(env_var: str) -> str:
     to be stamped by the run. Pinning it to a literal in source would label
     every future acquisition with the month someone happened to write it.
     """
-    return os.getenv(env_var, "").strip() or datetime.now(timezone.utc).strftime("%Y-%m")
+    return os.getenv(env_var, "").strip() or (
+        last_completed_month() if env_var == "RIS_RELEASE_KEY"
+        else datetime.now(timezone.utc).strftime("%Y-%m")
+    )
 
 
 def _nlsc_source_uri(release_key: str) -> str:
@@ -135,7 +145,7 @@ def _build_ris_nlsc_registry(
                 dataset_id=RIS_DATASET_ID,
                 release_key=ris_release_key,
                 release_kind=ReleaseKind.SNAPSHOT,
-                source_uri=RIS_SOURCE_URI,
+                source_uri=ris_source_uri(ris_release_key),
                 contract_version_id=RIS_CONTRACT_VERSION_ID,
                 released_at=None,
                 technical_readiness=TechnicalReadiness.INGESTION_VERIFIED,
@@ -200,21 +210,59 @@ def ris_population_release(
         base_timeout_seconds=external_sources.base_timeout_seconds,
         max_retries=external_sources.max_retries,
     )
-    raw_json, blob, execution = acquire_release_bytes(
-        kernel,
-        lease=build_lease(
-            provider_id=RIS_PROVIDER_ID,
-            purpose="Live RIS population snapshot ingestion",
+    source_uri = ris_source_uri(release_key)
+    capture_id = uuid4().hex
+    params = {key: os.environ[f"RIS_{key}"] for key in ("COUNTY", "TOWN", "VILLAGE")
+              if os.getenv(f"RIS_{key}", "").strip()}
+    max_pages = int(os.getenv("RIS_MAX_PAGES", "20"))
+    if not 1 <= max_pages <= 100:
+        raise ValueError("RIS_MAX_PAGES must be between 1 and 100")
+    page_receipts: list[dict[str, Any]] = []
+    all_rows: list[dict[str, Any]] = []
+    expected_pages: int | None = None
+    for page in range(1, max_pages + 1):
+        raw_json, blob, execution = acquire_release_bytes(
+            kernel,
+            lease=build_lease(
+                provider_id=RIS_PROVIDER_ID,
+                purpose="Live RIS population snapshot ingestion",
+                scope_principal_id=SCOPE_PRINCIPAL_ID,
+            ),
+            dataset_version_id=dataset_version_id,
+            execution_id=f"exec.ris.population.{release_key}.{capture_id}.{page}",
             scope_principal_id=SCOPE_PRINCIPAL_ID,
-        ),
-        dataset_version_id=dataset_version_id,
-        execution_id=f"exec.ris.population.{release_key}",
-        scope_principal_id=SCOPE_PRINCIPAL_ID,
-        url=RIS_SOURCE_URI,
-        blob_id=f"blob.ris.population.{release_key}",
-        storage_uri=f"ris://population/{release_key}.json",
-        headers=optional_credential("RIS_API_KEY"),
-    )
+            url=source_uri,
+            params={**params, "PAGE": str(page)},
+            blob_id=f"blob.ris.population.{release_key}.{capture_id}.{page}",
+            storage_uri=f"ris://population/{release_key}/{capture_id}/{page}.json",
+            headers=optional_credential("RIS_API_KEY"),
+            result_limit=1000,
+            licence="https://data.gov.tw/license",
+        )
+        payload = json.loads(raw_json)
+        rows = ris_response_rows(payload)
+        pages = int(payload["totalPage"]) if isinstance(payload, dict) and "responseData" in payload else 1
+        actual_page = int(payload["page"]) if isinstance(payload, dict) and "responseData" in payload else 1
+        if actual_page != page or (expected_pages is not None and expected_pages != pages):
+            raise ValueError("RIS pagination changed during acquisition")
+        if pages > max_pages:
+            raise ValueError("RIS release exceeds configured page budget; no release published")
+        expected_pages = pages
+        all_rows.extend(rows)
+        page_receipts.append(live_receipt(
+            blob=blob, execution=execution, release_key=release_key,
+            source_uri=source_uri, parser_version=RIS_ADAPTER_VERSION,
+            quarantined_count=None,
+            extra={"page": page, "total_pages": pages, "request_params": {**params, "PAGE": str(page)}},
+        ))
+        if page == pages:
+            break
+    if not all_rows:
+        raise ValueError("RIS release contains no population records")
+    # Preserve each original HTTP body in evidence. This array is a derived parser
+    # input, explicitly linked to all page receipts, never called a provider blob.
+    if len(page_receipts) > 1:
+        raw_json = json.dumps(all_rows, ensure_ascii=False).encode("utf-8")
 
     # Official totals are the provider's own published figures. They cannot be
     # recovered from the payload being reconciled against them, so when they
@@ -228,7 +276,7 @@ def ris_population_release(
     manifest, valid_records, quarantined = adapter.ingest_release(
         raw_bytes=raw_json,
         release_key=release_key,
-        source_uri=RIS_SOURCE_URI,
+        source_uri=source_uri,
         evidence_store=store,
         official_total_population=official_population,
         official_total_households=official_households,
@@ -241,18 +289,29 @@ def ris_population_release(
         len(quarantined),
     )
 
+    parser_blob = store.get_blob(manifest.blob_id)
+    if parser_blob is None:
+        raise ValueError("RIS parser input has no retained evidence")
     receipt = live_receipt(
-        blob=blob,
+        blob=parser_blob,
         execution=execution,
         release_key=release_key,
-        source_uri=RIS_SOURCE_URI,
+        source_uri=source_uri,
         parser_version=RIS_ADAPTER_VERSION,
         quarantined_count=len(quarantined),
         official_totals={
             "official_total_population": official_population,
             "official_total_households": official_households,
         },
+        extra={"pages": page_receipts, "page_count": len(page_receipts),
+               "parsed_record_count": len(valid_records), "requested_scope": params,
+               "input_encoding": "retained-response" if len(page_receipts) == 1 else "derived-json-array",
+               "independent_totals_verified": official_population is not None and official_households is not None},
     )
+    if len(page_receipts) > 1:
+        receipt.update(acquisition_mode="LIVE_PAGINATED_DERIVATION", execution_id=None,
+                       parent_execution_ids=[page["execution_id"] for page in page_receipts],
+                       returned_count=len(all_rows), coverage_status="FULL", completeness="COMPLETE")
     return dg.MaterializeResult(
         metadata={
             "contract_id": manifest.contract_id,
diff --git a/src/oday_data_platform/external/acquisition/kernel.py b/src/oday_data_platform/external/acquisition/kernel.py
index 30d9a09..6d24b7e 100644
--- a/src/oday_data_platform/external/acquisition/kernel.py
+++ b/src/oday_data_platform/external/acquisition/kernel.py
@@ -582,6 +582,18 @@ class LiveAcquisitionKernel:
         except (UnicodeDecodeError, json.JSONDecodeError):
             pass
 
+        # RIS reports totalDataSize for the national release even when COUNTY/TOWN
+        # filters narrow the response. Page counters describe the requested scope;
+        # treating the national total as the filtered count invents continuation.
+        if isinstance(parsed, Mapping) and isinstance(parsed.get("responseData"), list):
+            from oday_data_platform.external.sources.ris.official import (
+                ris_response_rows,
+            )
+
+            rows = ris_response_rows(parsed)
+            page, pages = int(parsed["page"]), int(parsed["totalPage"])
+            return len(rows), len(rows) if pages == 1 else None, page < pages
+
         if isinstance(parsed, list):
             returned = len(parsed)
         elif isinstance(parsed, Mapping):
@@ -939,6 +951,14 @@ class LiveAcquisitionKernel:
             stored_at=datetime.fromisoformat(str(metadata["fetched_at"])),
         )
         metadata = dict(metadata)
+        shape_error: ValueError | None = None
+        try:
+            self._response_shape(content, metadata.get("response_headers", {}), result_limit, params)
+        except ValueError as error:
+            # Preserve both raw response and failed execution when a provider
+            # returns HTTP 200 with an invalid application-level envelope.
+            shape_error = error
+            metadata["source_health"] = SourceHealth.SOURCE_ERROR.value
         execution = self._build_execution(
             lease=lease,
             execution_id=execution_id,
@@ -962,6 +982,9 @@ class LiveAcquisitionKernel:
             duration_started=duration_started,
         )
         self.evidence_store.record_search_execution(execution)
+        if shape_error is not None:
+            raise AcquisitionFailure("Provider response failed schema validation", metadata=metadata,
+                                     response_content=content) from shape_error
         return content, blob, execution
 
 
diff --git a/src/oday_data_platform/external/sources/mof_business/adapter.py b/src/oday_data_platform/external/sources/mof_business/adapter.py
index 3d17404..f8de8b8 100644
--- a/src/oday_data_platform/external/sources/mof_business/adapter.py
+++ b/src/oday_data_platform/external/sources/mof_business/adapter.py
@@ -54,7 +54,7 @@ from oday_data_platform.external.sources.mof_business.parser import (
 
 def create_mof_registry_definitions(
     release_key: str = "2026-08",
-    source_uri: str = "https://data.gov.tw/dataset/mof_business",
+    source_uri: str = "https://eip.fia.gov.tw/OAI/api/businessRegistration",
     content_sha256: str | None = None,
     released_at: datetime | None = None,
     technical_readiness: TechnicalReadiness = TechnicalReadiness.DECLARED,
diff --git a/src/oday_data_platform/external/sources/mof_business/parser.py b/src/oday_data_platform/external/sources/mof_business/parser.py
index abdad2a..10dc118 100644
--- a/src/oday_data_platform/external/sources/mof_business/parser.py
+++ b/src/oday_data_platform/external/sources/mof_business/parser.py
@@ -121,12 +121,12 @@ def parse_mof_record(raw: dict[str, Any]) -> MOFBusinessRecord:
     if not ban.isdigit() or len(ban) != 8:
         raise ValueError(f"Invalid BAN in MOF record: {ban!r}")
 
-    name_val = _first_present(raw, "business_name", "Busi_Chi_Name", "營業人名稱", "營利事業名稱", "Company_Name")
+    name_val = _first_present(raw, "business_name", "businessNm", "Busi_Chi_Name", "營業人名稱", "營利事業名稱", "Company_Name")
     name = str(name_val or "").strip()
     if not name:
         raise ValueError(f"Missing business name for BAN {ban}")
 
-    addr_val = _first_present(raw, "registered_address", "Busi_Addr", "營業地址", "登記地址", "Company_Location")
+    addr_val = _first_present(raw, "registered_address", "businessAddress", "Busi_Addr", "營業地址", "登記地址", "Company_Location")
     addr = str(addr_val or "").strip()
     if not addr:
         raise ValueError(f"Missing registered address for BAN {ban}")
@@ -146,7 +146,12 @@ def parse_mof_record(raw: dict[str, Any]) -> MOFBusinessRecord:
         status_norm, is_active = normalize_business_status(status_raw)
 
     # Industry codes & names
-    industry_codes_raw = _first_present(raw, "industry_codes", "Busi_Item", "行業代號", default=())
+    industry_codes_raw = _first_present(raw, "industry_codes", "Busi_Item", "行業代號")
+    if industry_codes_raw is None:
+        industry_codes_raw = [
+            value for suffix in ("", "1", "2", "3")
+            if (value := _first_present(raw, f"industryCd{suffix}")) is not None
+        ]
     if isinstance(industry_codes_raw, str):
         industry_codes = tuple(c.strip() for c in re.split(r"[,;|\s]+", industry_codes_raw) if c.strip())
     elif isinstance(industry_codes_raw, (list, tuple)):
@@ -154,7 +159,12 @@ def parse_mof_record(raw: dict[str, Any]) -> MOFBusinessRecord:
     else:
         industry_codes = ()
 
-    industry_names_raw = _first_present(raw, "industry_names", "Busi_Item_Name", "行業名稱", default=())
+    industry_names_raw = _first_present(raw, "industry_names", "Busi_Item_Name", "行業名稱")
+    if industry_names_raw is None:
+        industry_names_raw = [
+            value for suffix in ("", "1", "2", "3")
+            if (value := _first_present(raw, f"industryNm{suffix}")) is not None
+        ]
     if isinstance(industry_names_raw, str):
         industry_names = tuple(n.strip() for n in re.split(r"[,;|\s]+", industry_names_raw) if n.strip())
     elif isinstance(industry_names_raw, (list, tuple)):
@@ -163,7 +173,7 @@ def parse_mof_record(raw: dict[str, Any]) -> MOFBusinessRecord:
         industry_names = ()
 
     # Capital
-    cap_raw = _first_present(raw, "capital_amount", "Capital", "資本額", "Capital_Stock_Amount")
+    cap_raw = _first_present(raw, "capital_amount", "capitalAmount", "Capital", "資本額", "Capital_Stock_Amount")
     capital_amount: Decimal | None = None
     if cap_raw is not None:
         if isinstance(cap_raw, (int, float, Decimal)):
@@ -177,13 +187,13 @@ def parse_mof_record(raw: dict[str, Any]) -> MOFBusinessRecord:
                 except (InvalidOperation, ValueError):
                     capital_amount = None
 
-    org_type_val = _first_present(raw, "organization_type", "Organization", "組織別", "Organization_Desc")
+    org_type_val = _first_present(raw, "organization_type", "businessType", "Organization", "組織別", "Organization_Desc")
     org_type = str(org_type_val).strip() if org_type_val is not None else None
 
     resp_val = _first_present(raw, "responsible_person", "Master_Name", "負責人姓名", "Responsible_Name")
     resp_person = str(resp_val).strip() if resp_val is not None else None
 
-    use_inv_raw = _first_present(raw, "use_uniform_invoice", "Use_Invoice", "使用統一發票")
+    use_inv_raw = _first_present(raw, "use_uniform_invoice", "isUseInvoice", "Use_Invoice", "使用統一發票")
     use_invoice: bool | None = None
     if use_inv_raw is not None:
         if isinstance(use_inv_raw, bool):
@@ -197,7 +207,7 @@ def parse_mof_record(raw: dict[str, Any]) -> MOFBusinessRecord:
             elif s in ("N", "n", "false", "False", "0", "免用", "否"):
                 use_invoice = False
 
-    est_date = roc_date_to_iso(_first_present(raw, "established_date", "Setup_Date", "設立日期", "Approved_Date"))
+    est_date = roc_date_to_iso(_first_present(raw, "established_date", "businessSetupDate", "Setup_Date", "設立日期", "Approved_Date"))
     chg_date = roc_date_to_iso(_first_present(raw, "change_date", "Chg_Date", "變更日期", "Change_Date"))
 
     return MOFBusinessRecord(
diff --git a/src/oday_data_platform/external/sources/ris/adapter.py b/src/oday_data_platform/external/sources/ris/adapter.py
index 062314f..0144616 100644
--- a/src/oday_data_platform/external/sources/ris/adapter.py
+++ b/src/oday_data_platform/external/sources/ris/adapter.py
@@ -30,6 +30,10 @@ from oday_data_platform.external.sources.ris.models import (
     RISReleaseManifest,
     RISValidationSummary,
 )
+from oday_data_platform.external.sources.ris.official import (
+    normalize_odrp014,
+    ris_response_rows,
+)
 
 
 class RISAdapter:
@@ -56,7 +60,7 @@ class RISAdapter:
             text = raw_payload.decode("utf-8-sig", errors="replace").strip()
             if text.startswith("[") or text.startswith("{"):
                 loaded = json.loads(text)
-                records = loaded if isinstance(loaded, list) else [loaded]
+                records = ris_response_rows(loaded)
             else:
                 reader = csv.DictReader(io.StringIO(text))
                 records = list(reader)
@@ -64,7 +68,7 @@ class RISAdapter:
             text = raw_payload.strip()
             if text.startswith("[") or text.startswith("{"):
                 loaded = json.loads(text)
-                records = loaded if isinstance(loaded, list) else [loaded]
+                records = ris_response_rows(loaded)
             else:
                 reader = csv.DictReader(io.StringIO(text))
                 records = list(reader)
@@ -82,7 +86,8 @@ class RISAdapter:
             rec_id = f"ris.{release_key}.row.{idx}"
             raw_dict = row if isinstance(row, dict) else {"raw_value": row}
             try:
-                parsed = self._parse_single_record(row, release_key, obs_time, rec_id)
+                normalized = normalize_odrp014(row, release_key) if isinstance(row, dict) else row
+                parsed = self._parse_single_record(normalized, release_key, obs_time, rec_id)
                 if parsed.admin_code in seen_admin_codes:
                     quarantined.append(
                         RISQuarantineRecord(
diff --git a/tests/sources/official_source_fixtures.py b/tests/sources/official_source_fixtures.py
index 113a81a..f5c5777 100644
--- a/tests/sources/official_source_fixtures.py
+++ b/tests/sources/official_source_fixtures.py
@@ -409,6 +409,6 @@ STUB_ROUTE_SETS["ris_nlsc"] = {
 # MOF and MOI publish bulk downloads. Their assets record acquisition identity
 # only -- no parsing, no counts -- so a well-formed empty body is enough here.
 STUB_ROUTE_SETS["mof_moi"] = {
-    "data.gov.tw": (200, json_bytes([])),
+    "eip.fia.gov.tw": (200, json_bytes([])),
     "plvr.land.moi.gov.tw": (200, b"serial,county\n"),
 }
diff --git a/tests/sources/test_official_live_acquisition.py b/tests/sources/test_official_live_acquisition.py
index ef51b73..0cfcbeb 100644
--- a/tests/sources/test_official_live_acquisition.py
+++ b/tests/sources/test_official_live_acquisition.py
@@ -405,7 +405,7 @@ def test_error_response_bodies_are_never_parsed_as_source_data(
 @pytest.mark.parametrize(
     ("asset", "node", "route_fragment"),
     [
-        (mof_business_registrations_raw, "mof_business_registrations_raw", "data.gov.tw"),
+        (mof_business_registrations_raw, "mof_business_registrations_raw", "eip.fia.gov.tw"),
         (moi_rental_transactions_raw, "moi_rental_transactions_raw", "plvr.land.moi.gov.tw"),
         (ris_population_release, "ris_population_release", "ODRP014"),
     ],
