# ODP-MAP-E2E-001..004 Worker Evidence

Generated by: ODay Plus fleet worker, lane `Live map provider gate`
Workspace: `/home/lupin/odayplus-dev`
Generated at: 2026-06-29

## Scope

This evidence covers the repo-side implementation and verification for:

- `ODP-MAP-E2E-001` Live tile/geocoder boundary gate
- `ODP-MAP-E2E-002` Layer toggle URL persistence
- `ODP-MAP-E2E-003` Direct map picking
- `ODP-MAP-E2E-004` Deck semantic pixel proof

It does not claim remote-staging live map rollout completion. Remote-staging live tile/geocoder proof still requires real staging endpoint configuration and smoke evidence.

## Preflight

Passed in `/home/lupin/odayplus-dev`:

```bash
pwd
test -d apps/web/features/expansion
test -f tests/e2e/e2e-map.spec.ts
test -f docs/evidence/fleet_dispatch/ODP-MAP-E2E-001.md
```

Observed workspace:

```text
/home/lupin/odayplus-dev
```

## Release Authority Check

```bash
gh pr view 82 --json headRefOid,isDraft,state,mergeable,statusCheckRollup,url
```

Original handback observed:

- PR: `https://github.com/alfloop-dev/odayplus/pull/82`
- original handback headRefOid: `1494e51f7c90a35abbbc1b9feec6bb2dbb8d5633`
- `state`: `OPEN`
- `isDraft`: `true`
- `mergeable`: `MERGEABLE`
- Attached checks returned `SUCCESS` for `ci`, `product-e2e-gate`, `e2e-operational-evidence`, api/web build-and-publish, and deploy.

Current authority refresh:

- Refreshed: 2026-06-30
- Current PR #82 headRefOid: `fca45b70ee7c0e58278bcf734f82e2d5e83e373a`
- Current PR #82 state: draft/open/mergeable with attached checks successful at refresh time.

## Repo-Side Implementation Evidence

### ODP-MAP-E2E-001

Evidence in current repo:

- `apps/web/features/map/HeatZoneMap.tsx` reads map boundary configuration from query/env:
  - `mapTileUrl` / `NEXT_PUBLIC_ODP_MAP_TILE_URL`
  - `geocoderUrl` / `NEXT_PUBLIC_ODP_GEOCODER_URL`
  - `mapAttribution` / `NEXT_PUBLIC_ODP_MAP_ATTRIBUTION`
  - `mapTermsUrl` / `NEXT_PUBLIC_ODP_MAP_TERMS_URL`
- Runtime UI displays tile/geocoder configuration, attribution, and terms in `data-testid="map-boundary-config"`.
- Tile and geocoder fault fixtures display `data-testid="map-boundary-alert"` with correlation id.
- List/ranking/detail drawer remains available during map boundary faults.

Verified by:

```bash
npx playwright test tests/e2e/e2e-map-live-boundary.spec.ts --project=chromium --retries=1
```

Result: `3 passed`.

### ODP-MAP-E2E-002

Evidence in current repo:

- `HeatZoneMap.tsx` supports layer keys:
  - `h3`
  - `listings`
  - `candidates`
  - `confidence`
  - `freshness`
  - `risk`
- Layer toggles update the `layers=` URL state.
- Reload and shared URL restore layer visibility.

Verified by:

```bash
npx playwright test tests/e2e/e2e-map.spec.ts --project=chromium -g "layer toggles" --retries=1
```

Result: `1 passed`.

### ODP-MAP-E2E-003

Evidence in current repo:

- `HeatZoneMap.tsx` has direct pick handling for:
  - H3 zones
  - listing points
  - candidate site points
- Map picks route to the same drawer-backed state as list fallback:
  - `/w/expansion/heatzone?selected=...&drawer=zone`
  - `/w/expansion/listings?selected=...&drawer=listing`
  - `/w/expansion/candidates?selected=...&drawer=candidate`
- Selected state remains visible in map and ranked list.

Verified by:

```bash
npx playwright test tests/e2e/e2e-map.spec.ts --project=chromium -g "direct picking" --retries=1
```

Result: `1 passed`.

### ODP-MAP-E2E-004

Evidence in current repo:

- `tests/e2e/e2e-map.spec.ts` performs semantic pixel checks, not only nonblank canvas checks.
- Pixel proof covers:
  - listing point color
  - candidate point color
  - selected H3 boundary color
  - confidence layer visual delta
  - freshness layer visual delta
- Pixel proof fails if rendered deck layer state stops changing.

Verified by:

```bash
npx playwright test tests/e2e/e2e-map.spec.ts --project=chromium --retries=1
```

Result: `5 passed`.

## Dispatch Brief Validation

```bash
for task in ODP-MAP-E2E-001 ODP-MAP-E2E-002 ODP-MAP-E2E-003 ODP-MAP-E2E-004; do
  python3 scripts/e2e/check_product_grade_fleet_dispatch.py --task "$task" || exit 1
done
```

Result: passed for all four tasks.

## Completion Boundary

Repo-side map implementation and deterministic/product E2E for these four tasks are present and passing in `/home/lupin/odayplus-dev`.

Not completed here:

- remote-staging live tile endpoint smoke
- remote-staging live geocoder endpoint smoke
- staging evidence proving configured live endpoints with PR #82 release SHA
- any provider secret or credential setup

Those items remain external/staging blockers and must be evidenced separately.
