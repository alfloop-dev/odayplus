# ODP-P10-LIVE-EXTDATA-DIAG-001 — CI repair record (2026-08-10)

Owner: Claude · Reviewer: Antigravity · Phase: Package10LiveClosure

The orchestrator requeued this task twice on 2026-08-10 with
`ci_repair_requeued` (`11:09:50Z`, `11:12:27Z`): *"CI checks for task
ODP-P10-LIVE-EXTDATA-DIAG-001 failed; owner requeued to repair CI before
re-review."*

**Outcome: the red check was an environment-sensitive failure in an unrelated
product SLO test. It was repaired by re-running the job — no source change, and
none was permitted or needed.** The task's own deliverable was never implicated.

---

## 1. What was red

CI was evaluating task head
[`29d9321d`](https://github.com/alfloop-dev/odayplus/commit/29d9321d10232cd31b3ab18a9cdc4fa9a2bd5836)
(`ODP-P10-LIVE-EXTDATA-DIAG-001: diagnose zero readable ingestion runs`), which
had already merged to `dev` as PR
[#759](https://github.com/alfloop-dev/odayplus/pull/759) → merge commit
`9166d3ca`, `2026-08-09T16:44:41Z`.

| Check on `29d9321d` | State at requeue |
| --- | --- |
| `orchestrator` | success |
| `product` | success |
| `product-e2e-gate` | success |
| `performance-gate` | **failure** |
| `task-review-gate` (commit status) | **failure** |

## 2. The `performance-gate` failure

Run [31323018885](https://github.com/alfloop-dev/odayplus/actions/runs/31323018885),
job `93268847614`, completed `2026-08-09T16:10:07Z`:

```
FAILED tests/performance/assisted_listing_intake/test_capacity.py::test_approved_capacity_and_slo_are_measured
AssertionError: assert ['url_submission_durable_receipt',
                        'url_submission_receipt_error_budget'] == []
```

Both missed targets are **wall-clock latency budgets**, not correctness
assertions:

- `scripts/load/assisted_listing_intake/runtime.py:153` —
  `summarize("url_submission_durable_receipt", receipt_samples, 0.5, 1.5)`:
  P50 ≤ 0.5 s, P95 ≤ 1.5 s, measured over 120 rows submitted through a
  20-thread `ThreadPoolExecutor` against SQLite.
- `runtime.py:209` — `url_submission_receipt_error_budget` fires when more than
  1 % of those same receipt samples exceed target.

Both are therefore a direct function of CPU contention on the shared runner.
The failure landed on **attempt 3 of 3** of the gate's own internal retry loop
(`.github/workflows/ci.yml:177`), i.e. the workflow's built-in flake tolerance
was already exhausted by that day's contention.

## 3. Non-causality — five independent lines

1. **The task diff cannot reach the test.** `29d9321d` is docs-only: 3 files,
   +793/−0, every path under `docs/evidence/runtime/ODP-P10-LIVE-EXTDATA-DIAG-001/`.
   No file under `tests/`, `scripts/`, `apps/`, `modules/`, `shared/`, or
   `.github/` is touched. The task's `writable_paths` forbid all of them.
2. **The same failure occurs without the task commit.** Base parent
   `8eabc973` — which does *not* contain `29d9321d` — has `performance-gate`
   **failure** in run
   [31322265923](https://github.com/alfloop-dev/odayplus/actions/runs/31322265923)
   and **success** in run
   [31321422749](https://github.com/alfloop-dev/odayplus/actions/runs/31321422749).
   One SHA, both outcomes: the test is flaky independent of this task.
3. **The merged result is green.** Merge commit `9166d3ca` (dev + this change)
   passed `performance-gate` in both runs
   [31324581431](https://github.com/alfloop-dev/odayplus/actions/runs/31324581431)
   and [31323704453](https://github.com/alfloop-dev/odayplus/actions/runs/31323704453).
4. **`dev` tip is green.** `d37e6e5c` — which contains this task's change —
   reports `performance-gate` success.
5. **Local reproduction.** `test_approved_capacity_and_slo_are_measured` run
   10× on the worker host at this tree: **10 passed, 0 failed**.

## 4. Repair performed

Re-ran the failed job on the **unmodified** commit `29d9321d`
(`gh run rerun 31323018885 --failed`). Run 31323018885 now reports
`conclusion=success`.

Check-runs on `29d9321d` after repair — all green:

| Check | State |
| --- | --- |
| `orchestrator` | success |
| `performance-gate` | **success** |
| `product` | success |
| `product-e2e-gate` | success |

A red→green flip on a byte-identical tree with no intervening commit is itself
the conclusive flakiness proof; it also clears the orchestrator's CI-failure
requeue trigger.

## 5. Remaining red is not CI

`task-review-gate` stays `failure` because it mirrors **task status**, not code:
the `ci_repair_requeued` events moved this task back to `in_progress`, and the
gate only reports green for a task in a review-complete state. It clears on the
review transition accompanying this record, and needs no code action.

## 6. No patch in T10 — flake stabilisation is a separate T11 item

`tests/**`, `scripts/**`, and `.github/**` are all `forbidden_paths` for this
task, and the acceptance criterion *"code or config need produces exact T11
handoff and no patch in T10"* applies. The flaky test was therefore **not**
modified.

Recorded as a T11 candidate, **separate from and lower priority than** the
tenant-partition handoff in `README.md` §7 — it is CI hygiene, not a Package 10
live-closure blocker:

> **Stabilise `assisted_listing_intake` capacity SLO measurement against runner
> contention.** `test_approved_capacity_and_slo_are_measured` asserts absolute
> wall-clock P50/P95 budgets (0.5 s / 1.5 s) under 20-way concurrency on a
> shared GitHub runner, and exhausts its 3-attempt retry under load. Either
> gate the absolute-latency assertions behind a dedicated performance runner,
> or separate the *measurement* artifact from the *pass/fail* assertion so
> capacity is recorded on every run while only contention-independent
> invariants block CI. Owner should hold write access to
> `tests/performance/**` and `.github/workflows/ci.yml`.

## 7. Scope statement

No direct database write, no fabricated data, and no secret disclosure was
introduced by this repair. The only action taken against infrastructure was a
re-run of an existing CI job on an unchanged commit; the only file added is this
record, under the task's declared artifact path.
