# Security floor: fastapi 0.115.6 pinned starlette to 0.41.3, which carries
# PYSEC-2026-161/248/249/1941/1942/2280/2281. fastapi >= 0.138 requires
# starlette >= 0.46, so the explicit starlette floor below keeps the resolved
# version above every advisory fix version even if fastapi relaxes its bound.
# See docs/evidence/completion/ODP-ENG-DEPENDENCY-REMEDIATION-001/.
fastapi==0.138.1
starlette>=1.3.1
uvicorn[standard]==0.34.0
